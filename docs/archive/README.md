# Historical engineering notes

This directory contains pre-v1 implementation notes retained for project history and regression context.

They do **not** define the current installation, configuration, architecture, or operational contract. For v1.0 documentation, use the repository `README.md`, `RUNBOOK.md`, and the top-level files in `docs/`.
